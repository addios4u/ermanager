{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Extension",
            "type": "extensionHost",
            "request": "launch",
            "args": ["--extensionDevelopmentPath=${workspaceFolder}", "${workspaceFolder}/Users/addios4u/git/nestify"],
            "outFiles": ["${workspaceFolder}/out/**/*.js"],
            "preLaunchTask": "build"
        }
    ]
}
